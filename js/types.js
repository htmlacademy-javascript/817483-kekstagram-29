/** \
 * @typedef createPicture
 * @prop { number } id
 * @prop { string } url
 * @prop { string } descriptionArray
 * @prop { number } likes
 * @prop { Array } comments
 */

/** /
 * @typedef createPictureComment
 * @prop { number } id
 * @prop { string } avatar
 * @prop { Array } comments
 * @prop { string } NAMES
 */
